---
title: 概要
description: デモリソースとは何か、コンポーネントの選択方法、およびデプロイに必要な共通の Azure 前提条件について。
i18n:
  sourceHash: 7388ef989783
  translator: machine
---

デモリソースは、Terraform を使用してデプロイ可能な事前構成済みの Azure 仮想マシンです。
各コンポーネントは独立した VM であり、初回起動時に cloud-init を通じて
専用のソフトウェアがインストールおよび構成されます。

## コンポーネントの選択

コンポーネントはビルディングブロックです。デモの要件に基づいて選択してください：

- **オリジンサーバー**は、F5 XC セキュリティ機能のターゲットとなる脆弱な Web アプリケーションを提供します。ほとんどのデモで必要です。
- **トラフィックジェネレーター**は、デモ環境に対して攻撃トラフィックと正規トラフィックを生成します。WAF の有効性、ボット検出、または API 保護のデモに必要です。
- **CDN シミュレーター**は、クライアントとオリジン間に NGINX ベースの CDN エッジを配置し、ベンダー固有のヘッダーを挿入します。CDN 統合デモで使用されます。

## 共通の前提条件

すべてのコンポーネントに以下が必要です：

- コントリビューターアクセス権を持つ Azure サブスクリプション
- Terraform 1.5 以降
- Azure CLI の認証済み状態（`az login`）
- SSH キーペア（デフォルトパス `~/.ssh/id_ed25519.pub`）

## デプロイパターン

すべてのコンポーネントは同じ Terraform ワークフローに従います：

1. コンポーネントのリポジトリをクローン
2. `cd terraform/`
3. `terraform.tfvars.example` を `terraform.tfvars` にコピーし、必須変数を設定（`subscription_id` はすべてに必須。コンポーネントによっては追加の必須入力があります）
4. `terraform init && terraform plan && terraform apply`
5. コンポーネントのヘルスチェックエンドポイントで確認

デプロイヤー識別子は Azure AD アカウントから自動的に解決され、すべてのリソース名に埋め込まれます（例：`rg-origin-server-lab-rmordasiewicz`）。

## コンポーネント間の統合

コンポーネントを組み合わせることで、完全なデモ環境を構築できます：

```
Traffic Generator ──► F5 XC HTTP LB (WAF/Bot/API/CSD) ──► Origin Server
                                     ▲
                               CDN Simulator
```

**デプロイ順序：**

1. **オリジンサーバー** — 最初にデプロイします。他のすべてのコンポーネントがターゲットとするアプリケーションをホストします
2. **F5 XC HTTP ロードバランサー** — オリジンサーバーのパブリック IP を指すオリジンプールを構成し、WAF、Bot Defense、API Security、または Client-Side Defense ポリシーを持つ HTTP ロードバランサーを作成します
3. **トラフィックジェネレーター**および/または **CDN シミュレーター** — 最後にデプロイし、F5 XC ロードバランサーの FQDN を指定します

各コンポーネントの Terraform 変数は、以前にデプロイされたコンポーネントの出力を参照します。具体的な変数の要件については、各コンポーネントのドキュメントを参照してください。

現在のコスト見積もりには [Azure 料金計算ツール](https://azure.microsoft.com/en-us/pricing/calculator/) を使用してください。ラボがアイドル状態のときは `terraform destroy` を実行して課金を停止してください。
